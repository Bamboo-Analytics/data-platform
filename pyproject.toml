[project]
name = "data-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.0",
    "boto3>=1.34.0",
    "pyarrow>=15.0.0",
    "yfinance>=1.0",
    "dotenv>=0.9.9",
]

[tool.uv]
package=true

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
